# Kirjuta programm, mis küsib kasutajalt failinime kujul “failinimi.ext” (ext - extension - faili laiend) ja prindib välja laiendi (“ext”). (str.split)


name = input("Mis fail on? ")

name_split = nimi.split(".")

print(name_split[-1])